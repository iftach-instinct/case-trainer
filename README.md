# מאמן תיקים — התיק של נועם

אפליקציה סטטית בעברית וב־RTL לתרגול חשיבה מקצועית סביב מקרה **בדוי** של נועם, בן 8, עם קשיי קשב וקריאה. התוכן לימודי בלבד: הוא אינו מספק אבחנה, ציון, תוכנית טיפול או תחליף להערכה מקצועית ולהדרכה.

## הפעלה

פתחו את `index.html` בדפדפן עדכני. אין התקנה, תהליך בנייה, תלויות, גופנים חיצוניים או בקשות רשת מצד האפליקציה. ניתן גם להגיש את ארבעת קובצי האפליקציה מכל שרת קבצים סטטי. לדוגמה, אם Python מותקן:

```sh
python3 -m http.server 8000
```

לאחר מכן פתחו `http://localhost:8000`. התמיכה באחסון מקומי בכתובת `file://` משתנה בין דפדפנים; שרת מקומי מספק מקור יציב לשמירה.

## התרגול

1. **ההפניה:** תיאור המקרה, ניתוח שאלת ההפניה ואינטייק.
2. **העמקת הבירור:** מידע נוסף, שאלות לאינטייק ומסגרת CHC.
3. **אינטגרציה:** חשיבה מבדלת, היבטים רגשיים וסיכום ביניים זהיר.

בכל שלב מופיעים תיאור מקרה, שאלה ושדה כתיבה. כפתור החשיפה נפתח לאחר כתיבת 20 תווים לפחות; רווחים בתחילת הטקסט ובסופו אינם נספרים. אין ניתוח אוטומטי של איכות התשובה. השלב הבא נפתח רק לאחר לחיצה על כפתור החשיפה. מד ההתקדמות מייצג את מספר השלבים ששיקוליהם נחשפו.

אפשר לחזור לשלבים פתוחים בעזרת כפתורי הניווט או סרגל השלבים. עריכה שמקצרת תשובה מתחת לסף נועלת מחדש את השיקולים בשלב הזה ובשלבים שאחריו; הטקסטים שכבר נכתבו נשמרים. אחרי חשיפת השלב השלישי מוצג מסר סיום. האיפוס דורש אישור בחלונית, ומוחק את כל התשובות וההתקדמות של האפליקציה בלבד.

## שמירה ופרטיות

- התשובות, השלב הנוכחי ומצב החשיפה נשמרים ב־`localStorage` תחת `noam-case-trainer:v1`. הכתיבה נשמרת אחרי השהיה של 250 מילישניות, ביציאה מהשדה ובמעבר שלב; שמירה ממתינה נשלחת גם כשהדף מוסתר או עוזבים אותו.
- אין חשבון, שרת יישום, מעקב או העברת תשובות. האחסון המקומי אינו מוצפן על ידי האפליקציה; אין להזין מידע אישי או פרטים מזהים של ילדים אמיתיים.
- האחסון שייך לדפדפן ולמקור האתר: מעבר כתובת, דפדפן או מכשיר אינו מעביר את התרגול. אין סנכרון בין לשוניות; מומלץ לעבוד בלשונית אחת.
- כשהאחסון חסום או מלא מופיעה הודעה, ואפשר להמשיך בזיכרון של הדף. רענון או סגירה עלולים לאבד את הכתיבה. נתונים פגומים או בגרסה אחרת אינם מונעים שימוש.
- הטקסט שהמשתמש כתב מוכנס רק לערך של שדה הכתיבה; הוא אינו מפוענח כ־HTML. נתונים שמורים נבדקים לפני שחזור. אורך תשובה מוגבל ל־10,000 יחידות UTF-16 של הדפדפן.

## נגישות ועיצוב

HTML סמנטי, עברית ו־RTL, קישור דילוג, תוויות מפורשות לשדות, ניווט במקלדת, סימון מיקוד, התקדמות נגישה והודעות מצב. המיקוד עובר לתוכן החדש במעבר שלב ובחשיפה. חלונית האיפוס משתמשת ב־`dialog` המקורי של הדפדפן, ממקדת תחילה את הביטול ונסגרת גם באמצעות Escape; בדפדפנים ללא `showModal` נעשה שימוש באישור המקורי של הדפדפן. העיצוב מותאם למסכים קטנים, מצב כהה, צבעים מאולצים והעדפת תנועה מופחתת. מדובר במימוש תומך נגישות, ולא באישור עמידה בתקן.

## קבצים ובדיקות

- `index.html` — מעטפת המסך, ניווט וחלונית האיפוס.
- `content.js` — תוכן המקרה ושלושת השלבים; ניתן לערוך את הניסוחים כאן.
- `app.js` — הצגה, סף חשיפה, ניווט, בדיקת נתונים, שמירה ואיפוס.
- `styles.css` — העיצוב וההתאמות למובייל, נגישות ומצב כהה.

בדיקת תחביר עם Node, ללא התקנת חבילות:

```sh
node --check content.js
node --check app.js
```

בדיקת כללי הסף ושחזור מצב באמצעות הקוד שמשמש את האפליקציה:

```sh
node <<'NODE'
const assert = require('node:assert/strict');
const content = require('./content.js');
const app = require('./app.js');
assert.equal(content.stages.length, 3);
assert.equal(app.canReveal('א'.repeat(19)), false);
assert.equal(app.canReveal('א'.repeat(20)), true);
assert.equal(app.canReveal(' '.repeat(30)), false);
const state = app.createInitialState();
state.currentStage = 2;
assert.equal(app.normalizeState(state).currentStage, 0);
state.answers.fill('א'.repeat(20));
state.revealed.fill(true);
assert.deepEqual(app.normalizeState(state), state);
state.answers[1] = 'קצר';
const repaired = app.normalizeState(state);
assert.deepEqual(repaired.revealed, [true, false, false]);
assert.equal(repaired.currentStage, 1);
assert.deepEqual(app.normalizeState(null), app.createInitialState());
console.log('State checks passed');
NODE
```

בדיקה ידנית בדפדפן: כתבו 19 ואז 20 תווים, חשפו, נווטו עד לסיום, חזרו וערכו תשובה, רעננו ובדקו שחזור. בדקו גם ביטול ואישור איפוס, שימוש במקלדת, מסך צר, מצב כהה ואחסון חסום.
